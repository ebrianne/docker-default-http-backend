server {
  listen 80;

  location / {
        root   /usr/share/nginx/html;
        index index.php index.html index.htm;
    }

  location /healthz {
    access_log off;
    return 200 "healthy\n";
  }

  error_page 401 403 404 429 ./404.html;
}